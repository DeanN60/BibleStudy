import { OpenTrigger } from './open-trigger';

describe('OpenTrigger', () => {
  it('should create an instance', () => {
    const directive = new OpenTrigger();
    expect(directive).toBeTruthy();
  });
});
