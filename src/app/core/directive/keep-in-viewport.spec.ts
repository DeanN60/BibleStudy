import { KeepInViewport } from './keep-in-viewport';

describe('KeepInViewport', () => {
  it('should create an instance', () => {
    const directive = new KeepInViewport();
    expect(directive).toBeTruthy();
  });
});
