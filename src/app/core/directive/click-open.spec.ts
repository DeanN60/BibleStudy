import { ClickOpen } from './click-open';

describe('ClickOpen', () => {
  it('should create an instance', () => {
    const directive = new ClickOpen();
    expect(directive).toBeTruthy();
  });
});
